export default class OptimizelyHelper {
  
  trackEvent(eventName) {
    // EDITED OUT SO THAT KEN CAN DOUBLE CHECK

    // window['optimizely'] = window['optimizely'] || [];
    // window['optimizely'].push({
    //   type: "event",
    //   eventName: eventName,
    // });
  }

}